<div class="list-group rounded text-left">
    <h3 class="description section underline mb-1 mt-1">
        {{#if contributor.preparing}}{{application-loading size=1 class="d-inline"}}{{/if}} How to Contribute
    </h3>    
    <small class="text-muted mb-2"><strong>Last Commit</strong> {{moment-from-now contributor.updated_at}} {{#if contributor.commit_success}}({{contributor.commit_remote}}){{/if}}</small>
    
    <div class="row text-center top ml-0 mr-0 mt-0 rounded">
        <div class="col-sm-4 col-md-4">
            {{stacked-icon value="fa-clone" icon="fa-square" size="2" class="hidden-xs-down"}}
            <h4 class="line">{{stacked-icon value="fa-clone" icon="fa-square" size="1" class="hidden-sm-up d-inline"}} We've disguised and cloned this project to <strong>{{sessionAccount.account.github_username}}/{{contributor.repo}}</strong></h4>
        </div>
        <div class="col-sm-4 col-md-4">
            {{stacked-icon value="fa-wrench" icon="fa-square" size="2" class="hidden-xs-down"}}
            <h4 class="line">{{stacked-icon value="fa-wrench" icon="fa-square" size="1" class="hidden-sm-up d-inline"}} Make all of your changes to <strong>branch {{sprint_state.id}}</strong> of this repository with <a {{action "showGit"}}>Git</a> or <a target="_blank" href="https://github.com/{{sessionAccount.account.github_username}}/{{contributor.repo}}/tree/{{sprint_state.id}}">edit directly on Github</a><br><a {{action "showGit"}}>{{#if showingGit}}Hide{{else}}Show{{/if}}</a> Git instructions</h4>
        </div>
         <div class="col-sm-4 col-md-4">
            {{stacked-icon value="fa-rss" icon="fa-square" size="2" class="hidden-xs-down"}}
            <h4 class="line">{{stacked-icon value="fa-rss" icon="fa-square" size="1" class="hidden-sm-up d-inline"}} When you're ready <a {{action "submit" project.id contributor.id }}>submit your changes</a> and we will update your proposal</h4>
        </div>
    </div>

    {{#unless open}}
        {{application-error error="This phase is closed but you can improve your submission until a proposal is selected" class="mt-2"}}
    {{/unless}} 

    {{#if showingGit}}
    <div class="about section mb-2">
        If you have not cloned the repository
        <code class="line rounded">git clone -b {{sprint_state.id}} git@github.com:{{sessionAccount.account.github_username}}/{{contributor.repo}}.git</code>
    </div>
    <div class="about section">
        If you have previously cloned the repository
        <code class="line rounded">git fetch origin {{sprint_state.id}}; git checkout {{sprint_state.id}}</code>
    </div>
    {{/if}}
        
    <div class="about section mt-1">
        {{#if (eq sprint_state.state_id.name "requirements design")}}
        Add your changes to <a target="_blank" href="https://github.com/{{sessionAccount.account.github_username}}/{{contributor.repo}}/blob/{{sprint_state.id}}/requirements/Requirements-Document-for-Wired7-Sprint-v{{sprint.id}}.md">this requirements document file</a> (Requirements-Document-for-Wired7-Sprint-v{{sprint.id}}.md)
        {{/if}}
    </div>

        {{#if sprint_state.pull_request}}
        {{#unless sprint_state.merged}}
            <small class="line text-muted">Please resolve any merge conflicts related to this pull request by synchronizing your changes: <a target="_blank" href="https://github.com/{{org}}/{{project.name}}_{{project.id}}/pull/{{sprint_state.pull_request}}">{{project.org}}/{{project.name}} PR {{sprint_state.pull_request}}</a></small>
        {{/unless}}
        {{/if}}

</div>

<div class="text-left mt-2">
    <button class="btn btn-secondary btn-sm rounded" type="button" {{action "submit" project.id contributor.id }}>Submit Changes</button>
</div>

{{#if connections}}
    <h3 class="mb-0">{{connections.length}} Contacts</h3>
    <div class="about section mb-3">{{#if (eq team.plan.name "recruiter")}}Contact requests to talent that you invite are forwarded to your team instead of the candidate{{else}}Candidates you invite cannot see contact requests sent to them until their seat on your team expires{{/if}}</div>
    <div class="list-group rounded text-left">
        {{#each connections as |connection|}}
        <div class="list-group-item rounded">
            <div class="line">
                {{#if (eq connection.seat_id 4)}}
                {{#link-to "profile" connection.user_profile.id}}{{connection.first_name}} {{#if connection.user_profile.title}}({{user-profile-descriptor user_profile=connection.user_profile class="d-inline"}}){{/if}}{{/link-to}} is interested in your candidate, {{#link-to "profile" connection.contact_id}}{{connection.contact_first_name}}{{/link-to}}
                {{else if (eq connection.seat_id 5)}}
                Someone wants to contact your candidate, {{#link-to "profile" connection.contact_id}}{{connection.contact_first_name}}{{/link-to}}.  {{connection.contact_first_name}}'s seat expires <strong>{{moment-from-now connection.expires}}</strong>.
                {{/if}}
            </div>
            <small class="line text-muted">{{moment-from-now connection.created_at}}</small>
        </div>
        {{/each}}
    </div>

{{else}}
<div class="overview bg-nav rounded p-3 mb-2">
    <div class="text-left">
        <h3>No contacts</h3>
    </div>
</div>
{{/if}}

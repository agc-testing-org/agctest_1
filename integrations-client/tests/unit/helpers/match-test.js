
import { match } from 'integrations-client/helpers/match';
import { module, test } from 'qunit';

module('Unit | Helper | match');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = match([42]);
  assert.ok(result);
});


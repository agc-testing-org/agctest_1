{{#each items as |item|}}
    {{#if item.active}}
        <span {{action "updateItem" type item.id false namespace}} class="badge badge-default rounded selected cursor-pointer {{type}}">{{#if (eq type "role")}}<i class="fa fa-fw fa-1x {{item.fa_icon}}"></i>{{/if}} {{item.name}}</span>
    {{else}}
        <span {{action "updateItem" type item.id true namespace}} class="badge badge-default rounded unselected cursor-pointer {{type}}">{{#if (eq type "role")}}<i class="fa fa-fw fa-1x {{item.fa_icon}}"></i>{{/if}} {{item.name}}</span>
    {{/if}}
{{/each}}


import { previous } from 'integrations-client/helpers/previous';
import { module, test } from 'qunit';

module('Unit | Helper | previous');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = previous([42]);
  assert.ok(result);
});


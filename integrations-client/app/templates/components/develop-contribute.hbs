<div class="list-group rounded text-left">
    <div class="list-group-item active rounded justify-content-between">
        <h4>How to Contribute</h4>
        <button class="btn btn-secondary btn-sm rounded" {{action "submit" project.id contributor.id }}>Synchronize</button>
    </div>
    <div class="list-group-item rounded">
        <h4 class="line"><span class="badge badge-default rounded mr-1">Option 1</span> Command Line Access</h4>
        <p class="line">git clone -b {{sprint_state.id}} https://github.com/{{sessionAccount.account.github_username}}/{{contributor.repo}}.git</p>  
    </div>
    <div class="list-group-item rounded">
        <h4 class="line"><span class="badge badge-default rounded mr-1">Option 2</span> Github User Interface Access</h4>
        <p class="line"><a target="_blank" href="https://github.com/{{sessionAccount.account.github_username}}/{{contributor.repo}}/tree/{{sprint_state.id}}">{{sessionAccount.account.github_username}}/{{contributor.repo}} branch {{sprint_state.id}}</a></p>
        <small class="line text-muted">Use this option if you're less familiar with git and the command line</small>
    </div>
    {{#if (eq sprint_state.state.name "requirements design")}}
        <div class="list-group-item rounded">
            <h4 class="line">Requirements Document Placeholder</h4>
            <p class="line">We've created a file for your changes: <a target="_blank" href="https://github.com/{{sessionAccount.account.github_username}}/{{contributor.repo}}/blob/{{sprint_state.id}}/requirements/Requirements-Document-for-Wired7-Sprint-v{{sprint.id}}.md">Requirements-Document-for-Wired7-Sprint-v{{sprint.id}}.md</a></p>
        </div>                                                                          
    {{/if}}
    <div class="list-group-item rounded">
        <h4 class="line">Synchronize (let us know when you're done)</h4>
        <p class="line">Last Commit: {{moment-from-now contributor.updated_at}} {{#if contributor.commit}}({{contributor.commit}}){{/if}}</p>
        {{#if sprint_state.pull_request}}
        {{#unless sprint_state.merged}}
            <small class="line text-muted">Please resolve any merge conflicts related to this pull request by synchronizing your changes: <a target="_blank" href="https://github.com/{{project.org}}/{{project.name}}/pull/{{sprint_state.pull_request}}">{{project.org}}/{{project.name}} PR {{sprint_state.pull_request}}</a></small>
        {{/unless}}
        {{/if}}
    </div>
</div>


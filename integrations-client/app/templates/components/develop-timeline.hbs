<nav class="col-sm-12 col-md-4 hidden-xs-down bg-faded sidebar">
    <ul class="nav nav-pills flex-column">
        <li class="nav-item create">
            <a class="nav-link" {{action "showCreate"}}>{{#if displayCreate}}Cancel{{else}}Create{{/if}}</a>
        </li>
        {{#if displayCreate}}
        <li class="nav-item timeline p-4">
            {{#if errorMessage}}
            <div class="alert alert-warning rounded">{{errorMessage}}</div>
            {{/if}}
            <div class="form-group">
                <label>Title</label>
                {{input value=title placeholder="Briefly describe the request" class="form-control input-lg rounded"}}
            </div>                         

            <div class="form-group">           
                <label>Description</label>
                {{textarea value=description placeholder="Explain the request in detail" class="form-control input-lg rounded"}}
            </div>              
            <button {{action "createSprint"}} type="submit" class="btn btn-outline-secondary btn-lg rounded">Save Sprint</button>
        </li>
        {{/if}}
    </ul>

    <ul class="nav nav-pills flex-column">
        {{#each model.project.timeline as |timeline|}}
            {{timeline.sprint.title}}
        {{/each}}

        <li class="nav-item timeline">
            <div class="title">Implement Sinatra API endpoint that accepts a user's email address</div>
            <div class="description">
                The front-end team is building a splash page for our web application.

                The team will POST (application/json) an email address to a '/registrations' route.

                Please create this route and save the email address in a 'users' table in MySQL, with the following fields:

                email
                id
                created_at
                updated_at
            </div>
            <div class="change">
                status changed from 
                <span class="badge badge-default rounded">idea</span>
                to 
                <span class="badge badge-default rounded">backlog</span>
            </div>
        </li>
    </ul>
</nav>


    <div class="full">
        <div class="list-group rounded text-left">
            <div class="list-group-item active rounded">
                <h4>My Notifications</h4>
            </div>

            {{#if notifications}}
            {{#each notifications as |notification|}}
                <div class="list-group-item rounded highlight-{{notification.read}}">                    
                    {{#if (eq notification.diff "comment")}}
                        <p class="line">
                            {{#if notification.user_profile.title}}A {{user-profile-descriptor user_profile=notification.user_profile class="d-inline"}}{{else}}Someone{{/if}} commented on the {{#link-to "develop.project.sprint.state" notification.project.org notification.project.id notification.sprint.id notification.sprint_state.id}}{{notification.sprint_state.state_id.name}} phase{{/link-to}} of {{#link-to "develop.project.sprint" notification.project.org notification.project.id notification.sprint.id}}{{notification.sprint.title}}{{/link-to}}
                        </p>
                        <p class="line quotation">
                            <i class="fa fa-fw fa-1x fa-quote-left" aria-hidden="true"></i> {{notification.comment.text}} <i class="fa fa-fw fa-1x fa-quote-right" aria-hidden="true"></i>
                        </p>

                        <p class="line">Chime in!</p>
                    {{/if}}

                    {{#if (eq notification.diff "vote")}}
                        <p class="line">
                            {{#if notification.user_profile.title}}A {{user-profile-descriptor user_profile=notification.user_profile class="d-inline"}}{{else}}Someone{{/if}} voted on the {{#link-to "develop.project.sprint.state" notification.project.org notification.project.id notification.sprint.id notification.sprint_state.id}}{{notification.sprint_state.state_id.name}} phase{{/link-to}} of {{#link-to "develop.project.sprint" notification.project.org notification.project.id notification.sprint.id}}{{notification.sprint.title}}{{/link-to}}
                        </p>
                    {{/if}}
                   
                    {{#if (eq notification.diff "new")}}
                        <p class="line">
                            {{#if notification.user_profile.title}}{{user-profile-descriptor user_profile=notification.user_profile class="d-inline"}}{{else}}Someone{{/if}} created a new sprint ({{#link-to "develop.project.sprint" notification.project.org notification.project.id notification.sprint.id}}{{notification.sprint.title}}{{/link-to}}) for {{#link-to "develop.project" notification.project.org notification.project.id class="text-uppercase"}}{{notification.project.org}} / {{notification.project.name}}{{/link-to}}
                        </p>
                    {{/if}}

                    {{#if (eq notification.diff "transition")}}
                        <p class="line">
                            {{#link-to "develop.project.sprint" notification.project.org notification.project.id notification.sprint.id}}{{notification.sprint.title}}{{/link-to}} transitioned to the {{#link-to "develop.project.sprint.state" notification.project.org notification.project.id notification.sprint.id notification.sprint_state.id}}{{notification.sprint_state.state_id.name}} phase{{/link-to}}.  {{notification.sprint_state.state_id.instruction}}
                        </p>
                    {{/if}}

                    {{#if (eq notification.diff "winner")}}
                        <p class="line">
                            A contribution from the {{notification.sprint_state.state_id.name}} phase of {{#link-to "develop.project.sprint.state" notification.project.org notification.project.id notification.sprint.id notification.sprint_state.id}}{{notification.sprint.title}}{{/link-to}} was selected for merge into {{#link-to "develop.project" notification.project.org notification.project.id class="text-uppercase"}}{{notification.project.org}} / {{notification.project.name}}{{/link-to}}
                        </p>
                    {{/if}}

                    <small class="line text-muted">
                    
                        {{#if (eq notification.read false)}}{{notification-read notification_id=notification.id class="d-inline"}}{{/if}}
                        {{moment-from-now notification.created_at}}
                    
                    </small>
                </div>
            {{/each}}
            {{else}}
                <div class="list-group-item rounded highlight-false">
                    <h3 class="line">Welcome to Wired7!</h3>
                    <p class="line">We're glad you joined!  We designed our platform to make it easy for development team stakeholders to contribute to projects and receive legitimate feedback on their work with no strings attached.  Use the platform to learn, grow, or get noticed.</p>  
                    <p class="line">Here are some tips to help you get started:</p>
                    <p class="line">Your profile (current page) is <span class="text-warning"><strong>anonymous</strong></span>.  It displays your name and other non-specific data that you add but hides your name when viewed by other users.</p>  
                    <p class="line">We use the information you add to your profile to notify you of events you might be interested in.</p>  
                    <p class="line">As you contribute (through commits), the feedback that you receive will be aggregated on your profile.  We tag all feedback (comments and votes on contributions) with high-level information about users to give you (and others) a basic idea of the professional experience of the reviewer.</p>
                    <p class="line"></p>
                </div>
            {{/if}}
        </div>
    </div>

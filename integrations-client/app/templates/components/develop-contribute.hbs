<div class="list-group rounded text-left">
    <h3 class="description section text-capitalize underline mb-2 mt-1">
        {{#if contributor.preparing}}{{application-loading size=1 class="d-inline"}}{{/if}} How to Contribute
    </h3>
    {{#unless open}}
        {{application-error error="This phase is closed but you can improve your submission until a proposal is selected" class="mt-2"}}
    {{/unless}}
    <small class="text-muted mb-2">Last Commit: {{moment-from-now contributor.updated_at}} {{#if contributor.commit_success}}({{contributor.commit_remote}}){{/if}}</small>
    <div class="about section">
         We've disguised and cloned this project to <strong>{{sessionAccount.account.github_username}}/{{contributor.repo}}</strong>
         <br>Push all of your changes to this repository 
         <br>When you're ready <a {{action "submit" project.id contributor.id }}>submit your changes</a> to Wired7 and we will update your proposal
        {{#if (eq sprint_state.state_id.name "requirements design")}}
            <br>For this particular phase we have created <a target="_blank" href="https://github.com/{{sessionAccount.account.github_username}}/{{contributor.repo}}/blob/{{sprint_state.id}}/requirements/Requirements-Document-for-Wired7-Sprint-v{{sprint.id}}.md">a requirements document file</a> for your work 
        {{/if}}
    </div>
    <div class="list-group-item rounded">
        <h4 class="line"><span class="badge badge-default rounded mr-1">Option 1</span> Command Line Access</h4>
        <p class="line">git clone -b {{sprint_state.id}} git@github.com:{{sessionAccount.account.github_username}}/{{contributor.repo}}.git</p>
        <small class="line text-muted">Use this option if you have not cloned the repository</small>
        <p class="line">git fetch origin {{sprint_state.id}}; git checkout {{sprint_state.id}}</p>
        <small class="line text-muted">Use this option if you have previously cloned the repository</small>
    </div>
    <div class="list-group-item rounded">
        <h4 class="line"><span class="badge badge-default rounded mr-1">Option 2</span> Github User Interface Access</h4>
        <p class="line"><a target="_blank" href="https://github.com/{{sessionAccount.account.github_username}}/{{contributor.repo}}/tree/{{sprint_state.id}}">{{sessionAccount.account.github_username}}/{{contributor.repo}} branch {{sprint_state.id}}</a></p>
        <small class="line text-muted">Use this option if you're less familiar with git and the command line</small>
    </div>

        {{#if sprint_state.pull_request}}
        {{#unless sprint_state.merged}}
            <small class="line text-muted">Please resolve any merge conflicts related to this pull request by synchronizing your changes: <a target="_blank" href="https://github.com/{{org}}/{{project.name}}_{{project.id}}/pull/{{sprint_state.pull_request}}">{{project.org}}/{{project.name}} PR {{sprint_state.pull_request}}</a></small>
        {{/unless}}
        {{/if}}

</div>

<div class="text-left mt-2">
    <button class="btn btn-secondary btn-sm rounded" type="button" {{action "submit" project.id contributor.id }}>Submit Changes</button>
</div>

{{#if (and (or (eq user_profile.id sessionAccount.account.id) (eq user_id sessionAccount.account.id)) (eq disable_you null))}} <!-- check user_id for new -->
    you
{{else}}
{{#if user_profile.title}} 
    {{#if with_a}}a {{/if}} {{user_profile.title}} {{#if user_profile.industry}}in {{user_profile.industry}}{{/if}}{{#if user_profile.location}}{{#if multiple_lines}}<br>{{else}}, {{/if}}<i class="fa fa-map-marker" aria-hidden="true"></i> {{user_profile.location}}{{/if}}
{{else}}
    someone
{{/if}}
{{/if}}
